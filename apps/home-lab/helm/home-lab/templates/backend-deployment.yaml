apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "home-lab.fullname" . }}-backend
  labels:
    {{- include "home-lab.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  replicas: {{ .Values.backend.replicas }}
  selector:
    matchLabels:
      {{- include "home-lab.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: backend
  template:
    metadata:
      labels:
        {{- include "home-lab.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: backend
      annotations:
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
    spec:
      {{- include "home-lab.imagePullSecrets" . | nindent 6 }}
      containers:
        - name: backend
          image: "{{ .Values.backend.image.repository }}:{{ .Values.backend.image.tag }}"
          imagePullPolicy: {{ .Values.backend.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.backend.port }}
              protocol: TCP
          env:
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "home-lab.fullname" . }}
                  key: jwt-secret
            - name: GOOGLE_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "home-lab.fullname" . }}
                  key: google-client-id
            - name: GOOGLE_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "home-lab.fullname" . }}
                  key: google-client-secret
            - name: GOOGLE_REDIRECT_URI
              value: {{ .Values.backend.google.redirectUri | quote }}
            - name: GOOGLE_CALENDAR_ID
              value: {{ .Values.backend.google.calendarId | quote }}
            - name: NEWS_SOURCES_FILE
              value: "/app/config/news_sources.json"
            - name: GOCARDLESS_SECRET_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "home-lab.fullname" . }}
                  key: gocardless-secret-id
            - name: GOCARDLESS_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "home-lab.fullname" . }}
                  key: gocardless-secret-key
            - name: GOCARDLESS_REDIRECT_URI
              value: {{ .Values.backend.gocardless.redirectUri | quote }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.backend.resources | nindent 12 }}
          volumeMounts:
            {{- if .Values.persistence.enabled }}
            - name: data
              mountPath: /app/data
            {{- end }}
            - name: news-sources
              mountPath: /app/config
      volumes:
        {{- if .Values.persistence.enabled }}
        - name: data
          persistentVolumeClaim:
            claimName: {{ include "home-lab.fullname" . }}-data
        {{- end }}
        - name: news-sources
          configMap:
            name: {{ include "home-lab.fullname" . }}-news-sources
